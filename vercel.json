{
  "version": 1.1,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node",
      "config": { "runtime": "nodejs18.x" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.ts" }
  ]
}
